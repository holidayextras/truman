language: node_js
node_js:
- '4.1'
script: npm run $TEST_STEP
env:
  matrix:
  - TEST_STEP=build
  - TEST_STEP=lint
  - TEST_STEP=test
  - USE_CLOUD=true TEST_STEP=karma
notifications:
  email: false
